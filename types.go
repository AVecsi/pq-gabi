package gabi

import "github.com/AVecsi/pq-gabi/credtypes"

type Signature = credtypes.Signature
type SignatureProof = credtypes.SignatureProof
type Credential = credtypes.Credential
type CredentialDisclosure = credtypes.CredentialDisclosure
type DisclosureProof = credtypes.DisclosureProof
